# Agent Instructions

- If you make any UI change, run the headless UI check:
  - Ensure the dev server is running at `http://127.0.0.1:4173/`.
  - Run `node aimanager-shot.mjs http://127.0.0.1:4173/ /tmp/aimanager-home.png`.
  - Confirm the command succeeded and report any errors (do not print the PNG path unless asked).
